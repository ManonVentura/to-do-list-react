import { createGlobalStyle } from "styled-components";

const GlobalStyle = createGlobalStyle`
  body {
    margin: 0;
    padding: 0;
    background: #F8EFD4;
    font-family: 'Bold';
    font-size: 20px;
  }

`;

export { GlobalStyle };
